<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    app:startDestination="@id/sm_home">

    <fragment
        android:id="@+id/sm_home"
        android:name="fragment.HomeFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/sm_subject"
        android:name="fragment.SubjectFragment"
        android:label="Subject"
        tools:layout="@layout/fragment_subject" />
    <fragment
        android:id="@+id/sm_exam"
        android:name="fragment.ExamFragment"
        android:label="Exam"
        tools:layout="@layout/fragment_exam" />
    <fragment
        android:id="@+id/sm_logout"
        android:label="Logout"
        tools:layout="@layout/fragment_logout"
        android:name="fragment.LogoutFragment" />

</navigation>